#create a new file
# to create n new file in Python use the open() method with one of the following parameters:

#"x" = create - will create a file, returns an error if file exists
#"a" = Append- will create a file if specified file does not exists
#"w" = Write- will create a file if file specified file does not exist

f = open("myfile.txt", "x") #create & open file 